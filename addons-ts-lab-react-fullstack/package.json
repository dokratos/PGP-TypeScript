{
  "name": "addons-ts-lab-react-fullstack",
  "version": "1.0.0",
  "description": "## Task Your task is to create a frontend to the API that you created the other day, aka `puppiesAPI`. This is a good app to put in your portfolio, so even though it does not have any design requirements, make it look nice. The application should be built with React and TypeScript",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint .",
    "build": "npx tsc",
    "start": "node dist/index.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/index.js\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "@typescript-eslint/eslint-plugin": "^5.48.0",
    "@typescript-eslint/parser": "^5.48.0",
    "concurrently": "^7.6.0",
    "eslint": "^8.31.0",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4"
  }
}
